/**
 * 接口配置
 * USE_MOCK = true  使用本地模拟数据（后端未启动时）
 * USE_MOCK = false 请求真实后端接口（device-server，默认 http://localhost:3000）
 */
export const USE_MOCK = false

export const BASE_URL = 'http://localhost:3000'
